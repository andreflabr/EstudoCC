#include<bits/stdc++.h>
#include "matematica.h"

int potencia (int base, int expoente){
    return pow(base,expoente);
}

int resto (int valor, int divisor){
    return fmod(valor,divisor);
}

int arredonda (float vlr){
    return floor(vlr);
}
